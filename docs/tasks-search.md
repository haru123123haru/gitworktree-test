# 検索機能 タスクリスト

**ブランチ名:** `feature/search`
**worktree:** `main-search`
**優先度:** 低

---

## 概要

タスクのタイトルと説明をキーワード検索し、素早く目的のタスクを見つけられるようにする。
インクリメンタル検索と検索結果のハイライト表示を実装する。

---

## タスク一覧

### 1. 基本検索機能

#### 1.1 検索UI
- [ ] 検索入力フィールドの追加
- [ ] 検索アイコンの配置
- [ ] クリアボタンの実装
- [ ] 検索結果件数の表示

#### 1.2 検索ロジック
- [ ] タイトル検索の実装
- [ ] 説明文検索の実装
- [ ] 大文字小文字を区別しない検索
- [ ] 部分一致検索

### 2. インクリメンタル検索

- [ ] 入力時にリアルタイム検索
- [ ] デバウンス処理（300ms程度）
- [ ] 検索中のローディング表示
- [ ] 検索結果なしの表示

### 3. 検索結果ハイライト

- [ ] マッチした文字列のハイライト表示
- [ ] ハイライトのスタイリング
- [ ] HTMLエスケープ処理との両立

### 4. 高度な検索（オプション）

#### 4.1 フィルター連携
- [ ] ステータスでの絞り込み
- [ ] カテゴリでの絞り込み（categories機能と連携）
- [ ] 期限範囲での絞り込み（deadline機能と連携）

#### 4.2 検索オプション
- [ ] 検索対象の選択（タイトルのみ/説明のみ/両方）
- [ ] 完全一致/部分一致の切り替え

### 5. パフォーマンス最適化

- [ ] 大量タスク時の検索速度確認
- [ ] 必要に応じてインデックス作成
- [ ] メモ化による再計算防止

### 6. スタイリング

- [ ] 検索バーのスタイル
- [ ] ハイライトのスタイル（背景色: #fff59d）
- [ ] 検索結果なし状態のスタイル
- [ ] レスポンシブ対応

### 7. テスト・検証

- [ ] 基本的なキーワード検索の動作確認
- [ ] インクリメンタル検索の動作確認
- [ ] ハイライト表示の確認
- [ ] 特殊文字を含む検索の確認
- [ ] 空白文字の扱い確認
- [ ] 検索クリア時の動作確認

---

## 検索ロジック例

```javascript
// デバウンス関数
function debounce(func, wait) {
  let timeout;
  return function(...args) {
    clearTimeout(timeout);
    timeout = setTimeout(() => func.apply(this, args), wait);
  };
}

// 検索処理
function searchTasks(tasks, query) {
  if (!query.trim()) return tasks;

  const lowerQuery = query.toLowerCase();
  return tasks.filter(task =>
    task.title.toLowerCase().includes(lowerQuery) ||
    task.description.toLowerCase().includes(lowerQuery)
  );
}

// ハイライト処理
function highlightText(text, query) {
  if (!query.trim()) return escapeHtml(text);

  const escaped = escapeHtml(text);
  const regex = new RegExp(`(${escapeRegex(query)})`, 'gi');
  return escaped.replace(regex, '<mark>$1</mark>');
}

// 正規表現エスケープ
function escapeRegex(string) {
  return string.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
}
```

---

## UI設計

```
┌─────────────────────────────────────────┐
│  🔍 Search tasks...              [×]    │
└─────────────────────────────────────────┘
           ↓
┌─────────────────────────────────────────┐
│  Found 3 tasks matching "meeting"       │
├─────────────────────────────────────────┤
│  □ Weekly <mark>meeting</mark> prep     │
│  □ <mark>Meeting</mark> notes           │
│  ☑ Team <mark>meeting</mark>            │
└─────────────────────────────────────────┘
```

---

## 完了条件

- [ ] キーワードでタスクを検索できる
- [ ] 入力中にリアルタイムで結果が更新される
- [ ] マッチした箇所がハイライト表示される
- [ ] 検索結果件数が表示される
- [ ] 検索をクリアして全タスク表示に戻れる
